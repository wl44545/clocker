export enum APPROUTES {
  HOME = '',
  ACCOUNT = "account",
  ADMIN = 'admin',
  USERS = 'users',
  SETTINGS = 'settings',
  TIMER = 'timer',
  PROJECTS = 'projects',
  CLIENTS = 'clients',
  REPORTS = 'reports'
}
